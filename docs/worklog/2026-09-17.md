# 2026-09-17 작업기록 — 검색품질 근본버그 수정 + 인스타 재연결 + 비즈니스 인증 5차 제출

집PC. 사용자 피드백 "한국 패션 여자 크리에이터 검색했는데 남자·외국인이 나온다"에서 출발해
**검색 AI 판정이 아예 죽어 있던 근본버그**까지 파냄. 인스타 다른계정 연결 실패·비즈니스 인증
4차 반려도 함께 처리. (관련: docs/worklog/2026-09-16.md, 메모리 scout-os-instagram-dm.)

## 1. 검색 결과에 성별·국적 안 맞는 계정이 섞이던 문제
증상: "한국 패션 **여자** 크리에이터" → 남자(@__hyunchangchoi)·외국(@royalessencelux_llc)이 뜸.
- **표면 원인**: 해시태그 생성(query.ts)·AI 판정(match.ts)이 주제(패션)만 보고 속성조건(성별·국적) 무시.
  - match.ts COMMON_TAIL에 "성별·국적/지역·연령 조건도 전부 충족해야 fit, 하나라도 어긋나면 reject" 규칙 추가.
  - query.ts COMMON_HEAD에 "요청의 성별·국적 조건을 해시태그·intent에 반영" 규칙 추가(여자데일리룩 등).
  - 커밋 **4ed86d5**.

## 2. ⭐ 진짜 근본버그 — AI 셀럽 판정이 30일간 한 번도 작동 안 함
DB 확인(Supabase): 최근 모든 campaign_results의 `ai_verdict`가 **전부 null (judged=0)**, 그런데
`ai_error`도 null. 즉 **AI 매칭이 조용히 빈 결과를 뱉고 Apify 원본이 무필터로 노출**되고 있었다.
이게 "여자 검색에 남자·외국인" + "스카우트 오류 너무 많다"의 진짜 뿌리.
- **원인**: 기본 모델 `gemini-flash-latest`(2.5)는 thinking(내부 추론)이 켜져 있어, 큰 판정 호출에서
  추론이 max_tokens(3200)를 다 먹고 JSON 출력이 잘림 → parseMatches 파싱 실패 → 빈 verdict map →
  applyAiMatch가 `verdicts.size===0`에서 조용히 return(에러 기록 없이). planSearch(400토큰, 작은 출력)만
  우연히 성공해 문제가 안 보였음.
- **근거**: c5a17104 캠페인 intent="협업 가능한 한국 여성 패션..."(정상 생성)인데 6개 전원 verdict null.
- **수정(커밋 b50af90)**:
  - openrouter.ts: Google 호출에 `reasoning_effort:'none'` 전송 → thinking 비활성화(판정 토큰을 추론이
    안 먹게). 200 OK인데 본문 비면 ''를 삼키지 말고 throw → run.ts가 대체 모델 폴백, 실패 시 ai_error 가시화.
  - status route: `verdicts.size===0`이면 조용히 return하지 말고 throw → ai_error 기록(가시화).
- 배포 READY 확인(dpl_8FGPZahQ..., scout-os.kr). **⚠️ 검증 미완**: 사용자 force 재검색 후 DB에서
  ai_verdict 실제로 찍히는지 확인 필요(다음 세션 첫 할 일). 1시간 캐시라 반드시 '최신결과로 재검색'.

## 3. 인스타 다른 계정 연결 실패 (`?instagram=fail`)
- Vercel 런타임 로그 원문: `[instagram] callback failed: Error: API access blocked.`
- 원인: 토큰 교환 단계에서 인스타가 그 계정에 토큰 거부. **앱이 개발 모드라 테스터로 등록된 계정만** 연결됨.
  새 계정(비테스터) → 차단. + 프로페셔널(비즈/크리에이터) 계정이어야 함.
- "로그인 창이 안 뜬다" = 브라우저에 그 새 계정이 로그인돼 있어 인스타가 로그인창 없이 그 계정으로 바로
  처리→차단. 해결=instagram.com에서 로그아웃 후 원계정(jdiel.official) 로그인 후 재연결(또는 시크릿창).
- ⚠️ 현재 `instagram_connections` 테이블 **0행** — 연결이 풀린 상태. jdiel.official 재연결 필요.

## 4. 비즈니스 인증 — 4차 반려 → 5차 제출
- 4차(수동검토) 반려: 상호·주소 문서 불일치. (3차 땐 상호·주소 통과였는데 뒤집힘 = Meta 자동판정 비일관.)
- 정확한 불일치: 등록증엔 우편번호(02489)·"Seoul"·"서울시"가 없는데 입력엔 있어서 "문서에 없는 정보"로 튕김.
  등록증 주소 = `서울특별시 동대문구 왕산로 239, 18층 1814호(청량리동, 힐스테이트청량리)`.
- **5차 제출** → **5차 반려**: 주소는 통과, 다시 "웹사이트에 법적이름 없음"(3차와 동일 사유).
  - curl -A facebookexternalhit 확인: **apex·www 둘 다 200 + '플로닛랩스' 노출 정상** → 사이트 문제 아님,
    **Meta 캐시 널뛰기 확정**(4차엔 웹사이트 통과였는데 5차엔 실패 = 같은 사이트 판정 오락가락).
  - 보강: 홈 푸터에 "본 웹사이트 Scout OS는 플로닛랩스가 운영합니다" 문장 추가(커밋 **0c39eb4**), 배포 확인.
- **6차 제출(2026-09-17)**: 웹사이트 보강 + 공유디버거 재스크랩 + 주소 등록증 정합 = 3개 동시 정렬 상태로 제출.
  문서=사업자등록증(이름·주소 둘 다), jpg→pdf 권장(한글 OCR).
- ✅✅ **비즈니스 인증 완료!! (2026-09-17)** — 보안센터에 "플로닛랩스 인증 · Sep 17, 2026 최초 인증됨 · 인증됨(초록체크)".
  6차 만에 통과. 최대 관문 돌파. 이제 App Review 고급액세스 신청 가능.

## 커밋(오늘)
- `4ed86d5` fix(discover): 검색 의도의 성별·국적 조건도 반영
- `b50af90` fix(ai): Gemini thinking이 판정 JSON을 잘라 매칭이 전혀 안 되던 근본버그

## ▶▶ 다음 세션 첫 할 일
1. ✅ 비즈니스 인증 **완료** — 이제 **App Review 마무리**: developers.facebook.com → Scout OS → 검수 → 앱 검수 →
   ③허용되는 사용 방법 ④데이터 처리 설문(Claude가 답변 작성) + ⑤검수자 지침(데모 스크린캐스트: 개인계정을
   Instagram 테스터 추가→jdiel.official에 DM→'받은 답장'에 뜨는 것 녹화) → 최종 제출 → **Live 게시 → 모든 고객 DM**.
2. **검색 AI 판정 검증**: '최신결과로 재검색'(force)으로 "한국 패션 여자 크리에이터" → DB에서 ai_verdict가
   채워지고 남자·외국이 reject 되는지 확인. (안 되면 reasoning_effort 미지원 가능성 → Vercel 로그 확인)
3. jdiel.official 인스타 재연결(instagram_connections 0행 상태 — 개발모드라 테스터 계정으로).
