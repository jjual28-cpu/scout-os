# 2026-09-15 작업기록 — 인스타 DM 자동수집(Meta 연동) 대장정

집PC에서 진행. 오늘의 목표: **핵심 기능 "찾기 → 자동 DM → 답장 자동수집"** 중
**Meta 인스타그램 연동(답장 자동수집)** 을 실제로 켜기. 코드는 이미 90% 있었고, **Meta 앱
설정·연결**이 관문이었다. 결과: **연결·구독까지 완성**, 남은 건 **App Review(게시)** 뿐.

## 1. Meta 개발자 계정 관문 (제일 오래 걸림)
- **비즈니스 계정(jdiel.official)로는 developers.facebook.com 진입 불가** — 계속 Business Suite로
  튕김. 개발자센터는 **개인 페이스북 프로필**로만 들어간다.
- 개인 FB 계정 신규 생성 → 개발자 등록. **신규계정+시크릿모드**가 Meta 보안에 걸림("평소 안 쓰는
  기기") → **일반 창 + 계정 묵히기**로 통과. About you = "개발자" 선택.

## 2. Scout OS 앱 생성 (Instagram API with Instagram Login)
- 앱 유형: 비즈니스. 이용 사례: **"Instagram에서 메시지 및 콘텐츠 관리"** 만(메신저·WhatsApp 제외).
- **Instagram 앱 ID: `1124990809957831`** (= `INSTAGRAM_APP_ID` / `NEXT_PUBLIC_INSTAGRAM_APP_ID`).
  시크릿은 **Vercel env에만**(git·문서에 안 올림).
- 권한: instagram_business_basic / _manage_comments / _manage_messages ("Add all required permissions").

## 3. Vercel 환경변수 4개 (Production)
- `INSTAGRAM_APP_ID` = 1124990809957831
- `NEXT_PUBLIC_INSTAGRAM_APP_ID` = 1124990809957831 — ⚠️ **Type=Config**(Secret 불가: NEXT_PUBLIC은 공개값)
- `INSTAGRAM_APP_SECRET` = (Vercel에 직접 입력, 값은 여기 안 적음)
- `INSTAGRAM_WEBHOOK_VERIFY_TOKEN` = `scout_ig_webhook_a7f3k9m2x8q4`
- 넣고 **Redeploy** 필수(env는 재배포해야 적용). 검증: `curl .../api/instagram/webhook?...verify...` → challenge 에코 확인.

## 4. Meta 콘솔 설정값 (도메인 주의!)
- **OAuth redirect URI**: `https://scout-os.kr/api/instagram/callback`
  (앱이 `NEXT_PUBLIC_APP_URL`(=apex `scout-os.kr`)로 만들어 보냄 → 이 값과 정확히 일치해야 함.)
- **Webhook 콜백 URL**: `https://www.scout-os.kr/api/instagram/webhook`
  ⚠️ **www 필수!** apex `scout-os.kr`은 **308→www.scout-os.kr 리다이렉트**라, Meta는 리다이렉트를
  안 따라가서 콜백은 반드시 **www 붙은 직접 200 응답 주소**를 넣어야 한다.
- **Verify token**: 위 `scout_ig_webhook_a7f3k9m2x8q4`.
- **Webhook 필드 "messages" 구독(Subscribe)** 체크(v26.0). — 이건 이미 되어 있었음.

## 5. jdiel.official 인스타 테스터 등록·수락
- 앱 역할 → 역할 → **Instagram 테스터** 탭 → "사람 추가" → 다이얼로그 맨 아래 **"Instagram 테스터"
  라디오 선택** → 검색칸에 `jdiel.official` → 추가. (자동완성에서 선택해야 저장됨.)
- **수락 위치가 함정**: 인스타 앱/설정 메뉴엔 안 뜬다(계정센터로 빠짐). **PC에서 직접 주소**
  `https://www.instagram.com/accounts/manage_access/` → "테스터 초대" 탭 → 수락. (계정은 공개여야 함.)

## 6. 연결 성공 + 버그 수정 (핵심)
- Scout OS 설정 → 인스타그램 연결 → jdiel.official 로그인·허용 → **연결 성공.**
  DB 확인: `instagram_connections` 에 username=jdiel.official, 토큰 유효(~2026-11-14), ig_user_id=28484421387886936.
- **버그**: "답장 수신 재연결"(및 OAuth 직후 자동구독)이 실패. 임시 로그(커밋 7f3316e)로 실제 응답
  확인: `POST /{ig_id}/subscribed_apps` → **code 100/33 "does not support this operation"**.
- **수정(커밋 99d9103)**: Instagram Login API는 경로에 숫자 ID를 넣으면 거부 → **`/me/subscribed_apps`**
  로 호출하도록 변경. → **"답장 수신이 켜졌어요" 성공(구독 200).**
- 진단 로그(subscribe 실패 시 응답 본문 console.error)는 유용해서 **유지**.

## 7. 미해결(코드 아님, Meta 정책) — 다음 단계
DM 테스트: 개인 계정(비테스터, jdiel 미팔로우)에서 jdiel.official로 DM → **받은 답장에 안 뜸.**
로그상 **웹훅 자체가 안 옴**. 원인 = **개발 모드에선 "테스터인 발신자"의 DM만 웹훅으로 온다.**
개인 계정이 테스터가 아니라 Meta가 안 쏜 것(우리 코드·연결·구독은 정상).

### ▶▶ 다음 세션 우선 작업 — "모든 고객 DM 받기" = 앱 Live 게시
공식 문서 확인: 모든 발신자 DM 수신 = `instagram_business_manage_messages` **고급 액세스** 필요 →
**Meta App Review + 비즈니스 인증 + Live 게시**. 며칠~1~2주 소요(Meta 심사).
1. **App Review 제출** — 데모 스크린캐스트(메시징 사용사례)·상세 설명·개인정보처리방침(있음: /privacy).
2. **비즈니스 인증** — 플로닛랩스 사업자 인증(사업자등록증 등). App Review 전에 완료돼야 함.
3. **앱 게시(Live)** 전환. (개발 모드는 고급 액세스 못 받음.)
- **오늘 미실행(선택)**: 개인 계정을 Instagram 테스터로 추가하면 심사 없이 5분만에 "작동 확인" 가능
  (파이프라인 정상 검증용). 사용자는 곧장 실서비스(App Review)로 가기로 함.
- UX 후순위: "인스타그램 연결" 버튼이 같은 창 리다이렉트라 Scout 창이 잠깐 사라짐 → 새 탭으로 다듬기.

## 커밋(오늘)
- `7f3316e` chore(diag): 인스타 웹훅 구독 실패 시 정확한 응답 본문 로깅
- `99d9103` fix(instagram): 웹훅 구독을 /me/subscribed_apps 로 호출 (code 100/33 해결)

## 참고 — 재개 시 빠른 체크
- 연결 상태: 설정 → 인스타그램 연결 = "@jdiel.official 연결됨".
- 웹훅 살아있나: `curl "https://www.scout-os.kr/api/instagram/webhook?hub.mode=subscribe&hub.verify_token=scout_ig_webhook_a7f3k9m2x8q4&hub.challenge=TEST"` → `TEST` 에코면 정상.
- 개발자센터: 개인 FB로 로그인해야 Scout OS 앱 보임(client_id 2191750468441482 — Meta 앱ID, 인스타 앱ID와 다름).
